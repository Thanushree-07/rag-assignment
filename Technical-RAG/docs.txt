Authentication API

Version 2.0 authentication uses token-based login.

Steps:
1. Send POST request to /login
2. Provide username and password
3. Server returns access token
4. Include token in Authorization header for future requests

Password Reset:
Users can reset password using /reset endpoint.
Email verification is required.

Errors:
401 → Invalid credentials
403 → Access denied
500 → Server error
